<div class="user-recipes">
  <h1 class="title">Public Recipes</h1>
  <ul>
    <% @public_recipes.each do |recipe| %>
      <li class="recipe">
        <div>
          <h2><a href="<%= public_path(recipe)%>"><%= recipe.name%></a></h2>
          <span>By <%= recipe.user.name %></span>
        </div>
        <a href="<%= public_path(recipe)%>">
          <p>Total food items: <%= recipe.recipe_foods.count %></p>
          <p>Total Price: $<%= RecipeFood.where(recipe_id: recipe.id).joins(:food).sum('recipe_foods.quantity * foods.price')%></p>
        </a>
      </li>
    <%end%>
  </ul>
</div>